version: 2

jobs:
  build:
      docker:
        - image: circleci/python:3
      steps:
        - checkout
        - run:
            name: install tox
            command: pip install tox
        - run:
            name: run tests
            command: tox

workflows:
  version: 2
  pytest-csv:
    jobs:
      - build
